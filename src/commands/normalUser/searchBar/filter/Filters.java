package commands.normalUser.searchBar.filter;

/**
 * Filters that can be applied for searching
 */
public enum Filters {
    name, album, lyrics, genre, artist,
    tags, releaseYear, owner, visibilityForUser, visibility
}
